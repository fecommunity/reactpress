#!/bin/sh
. "$(dirname "$0")/_/husky.sh"
. "$(dirname "$0")/scripts/hooks-common.sh"

# Cursor trailers or legacy author names may appear after commit-msg; amend once if needed.
fixup_last_commit_if_needed
